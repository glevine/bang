#!/bin/sh

BANG_SCRIPT_DIRECTORY=$(cd "$(dirname "$0")" && pwd -P)
BANG_SCRIPT_FILE="$BANG_SCRIPT_DIRECTORY/${0##*/}"
BANG_PATH=$(cd "$BANG_SCRIPT_DIRECTORY"/.. && pwd -P)
BANG_LIB=$(cd "$BANG_PATH"/lib && pwd -P)

exec "$BANG_LIB/bang.rb" "$@"
